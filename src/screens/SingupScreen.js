import React from "react";
import { View, Text, StyleSheet } from "react-native";

const SingupScreen = () => {
  return (
    <View>
      <Text style={{ fontSize: 48 }}>SingupScreen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default SingupScreen;
